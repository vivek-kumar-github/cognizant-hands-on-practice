# Spring Core Exercise 4

This folder contains a basic Maven project for a library application.

## What is included
- `pom.xml` with Spring Context dependency
- Maven compiler plugin configuration
- A simple Java entry point

## Project structure
- `src/main/java/com/example/library/LibraryApp.java`

## Build and run
You can build it with Maven once Maven is installed and available on your PATH.
